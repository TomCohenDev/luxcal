import 'package:flutter/material.dart';

Widget spacer(double height) => SizedBox(height: height);

Widget spacerWidth(double width) => SizedBox(width: width);

Widget spaceTaker() => Expanded(child: Container());
